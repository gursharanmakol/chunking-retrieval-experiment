# Stage 1 (inspect_chunks.py) needs nothing beyond the Python standard library.
# The packages below are required for the stage 2 retrieval run:
#   embeddings: sentence-transformers/all-MiniLM-L6-v2
#   cosine similarity over the chunk vectors
sentence-transformers==3.3.1
numpy==2.1.3
